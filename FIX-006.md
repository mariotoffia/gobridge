# FIX-006 — Refactor adapters into ACL files (promote `lint-acl` to enforcing)

> Carved out of `FINAL_DDD_HEX_CLEAN_FIX_PLAN.md` for a dedicated
> session. The `aclcheck` analyzer was built and wired up in commit
> `794f659` (FIX-006 phase 1) but runs as **advisory** because no
> existing adapter follows the `acl_*.go` naming convention. This
> document is the runbook for the per-adapter restructure that
> promotes `lint-acl` to a `make lint` gate.

## Why this exists

In Hexagonal Architecture, the Anti-Corruption Layer (ACL) is the
seam where an external SDK's vocabulary (its types, errors,
encodings) is translated into the project's domain types. Today this
translation is scattered across multiple files inside each adapter
(`receiver.go`, `headers.go`, `errors.go`, …) — future maintainers
cannot find the SDK boundary at a glance, and SDK types can leak
across the adapter without anyone noticing during review.

The DDD remedy: concentrate the SDK boundary in explicitly-named
files (`acl_*.go` or under an `acl/` sub-directory). Reviewers find
the seam in seconds; the `aclcheck` analyzer mechanically catches any
file that imports an SDK without being part of the ACL.

## Current state (snapshot at FIX-009)

- **Analyzer in place**: `scripts/aggcheck/` builds; `make build-aclcheck`
  produces `bin/aclcheck`.
- **Wired as advisory**: `make lint-acl` writes `reports/aclcheck.log`
  with the current violations and exits 0 regardless. It is also
  included in `make arch-quality` alongside `dupl`/`goconst`.
- **Not in `make lint`**: promoting it to a hard gate would fail every
  build until the per-adapter refactor lands.
- **Test files exempted** in the analyzer (tests legitimately
  construct SDK types for mocks and edge-case probes).

### Today's violation count by adapter (from `reports/aclcheck.log`)

```
  18 ./adapters/aws/transport/sqs
  13 ./adapters/aws/store        (factory aggregator)
  11 ./adapters/mqtt/transport/paho
   9 ./adapters/aws/metrics/cloudwatch
   8 ./adapters/azure/transport/servicebus
   7 ./adapters/otel/tracing
   7 ./adapters/aws/credentials/ssm
   7 ./adapters/aws/config/dynamodb
   7 ./adapters/amqp/transport/amqp10
   7 ./adapters/amqp/transport/amqp091
  (rest: native stores ~1–4 each, otel metrics, native config/file)
 ───
 113 total
```

The largest concentration is in transport adapters (sqs, paho,
servicebus, amqp091, amqp10) — those are also the most architecturally
load-bearing because they're the inbound/outbound SDK boundary.

## Target structure (per adapter)

The convention enforced by `aclcheck`:

- A file whose name starts with `acl` (e.g. `acl.go`,
  `acl_envelope.go`, `acl_inbound.go`) is part of the ACL — it MAY
  import the vendor SDK.
- A file in an `acl/` sub-directory of the adapter package is part of
  the ACL — it MAY import the vendor SDK.
- Everywhere else inside the adapter package, the SDK import is
  forbidden.

### Recommended per-adapter split (transport adapters)

```
adapters/<vendor>/transport/<tech>/
├── acl_inbound.go    # SDK message → domain.Envelope (parses headers,
│                     # extracts trace context, maps SDK error codes)
├── acl_outbound.go   # domain.Envelope → SDK send call (formats headers,
│                     # serialises payload, applies SDK-specific options)
├── acl_errors.go     # SDK error type → domain.BridgeError classification
├── acl_session.go    # (optional) SDK Session/Connection lifecycle if it
│                     # exposes SDK types in its API
├── receiver.go       # ports.Receiver implementation, no SDK imports —
│                     # delegates to acl_inbound for translation
├── sender.go         # ports.Sender implementation, no SDK imports —
│                     # delegates to acl_outbound
├── factory.go        # ports.TransportFactory wiring, no SDK imports
└── config.go         # option DTOs, no SDK imports
```

The pattern works for non-transport adapters too:
- **stores**: `acl_query.go` (SQL/DDB query construction), `acl_row.go`
  (row → domain mapping), `store.go` (port implementation).
- **credentials**: `acl_secret.go` (SDK GetSecretValue → domain.Credential),
  `repository.go` (port implementation).
- **metrics/tracing**: `acl_emit.go` (domain metric → SDK call),
  `exporter.go` (port implementation).

### When the split doesn't fit

Some types are inherently the SDK's, not the domain's. AMQP091's
`*amqp.Channel` is the SDK's resource; the receiver loop must hold
it for the lifetime of a subscription. Two options:

1. **Wrap it.** Put `*amqp.Channel` behind an unexported `channel`
   alias in `acl_session.go`; the receiver holds the alias. The SDK
   import stays in the ACL file, the receiver doesn't import the SDK.
2. **Accept the analyzer hit.** Add a per-file exemption in the
   analyzer's `vendorPatterns` for cases where wrapping adds no
   architectural value (purely lifecycle, no translation).

Prefer (1). Don't undermine the analyzer for convenience.

## Step-by-step approach

### Phase 0 — Preparation

1. Confirm baseline: `make check` is green at HEAD; `make lint-acl`
   produces `reports/aclcheck.log` with ~113 advisory hits.
2. Pick the adapter to start with. Recommended order (smallest →
   largest):
   1. `adapters/native/store/sqlite{outbox,dlq}` (~3 violations each;
      good practice run)
   2. `adapters/aws/credentials/ssm` (7 violations, contained scope)
   3. `adapters/aws/config/dynamodb` (7 violations)
   4. `adapters/amqp/transport/amqp091` and `amqp10` (7 each)
   5. `adapters/azure/transport/servicebus` (8)
   6. `adapters/aws/metrics/cloudwatch` (9)
   7. `adapters/mqtt/transport/paho` (11)
   8. `adapters/aws/transport/sqs` (18 — biggest, save for last)
   9. `adapters/otel/{metrics,tracing}` (variable)
3. Per-adapter, the work is mechanical but not automatable. Each
   adapter ships as one commit.

### Phase 1 — Per-adapter refactor (template)

For each adapter, repeat:

1. **Identify the SDK boundary points.** `grep` the adapter for the
   SDK import; for each file that imports it, decide:
   - Does this file translate SDK types ↔ domain types? → it IS the
     ACL; rename to `acl_*.go`.
   - Does this file just hold SDK resources internally? → wrap them
     in unexported types in `acl_session.go` or similar; remove the
     SDK import from the rest of the file.
   - Does this file expose SDK types in its public API? → that's a
     leak; redesign to use domain types.

2. **Move/rename files.** Use `git mv receiver.go acl_receiver.go`
   only when the file is genuinely an ACL. Most often, the file
   stays put and SDK uses move into a new `acl_*.go` sibling.

3. **Update unit tests.** Tests inside the adapter package need no
   change (tests are exempted by the analyzer). Tests in other
   packages that import adapter internals need API updates only if
   the public API changed.

4. **Verify.**
   ```bash
   cd adapters/<vendor>/transport/<tech>
   go build ./...
   go vet ./...
   go test -race -timeout 60s ./...
   cd -
   make lint-acl   # this adapter should now show 0 violations
   ```

5. **Commit.** Format: `FIX-006: refactor <adapter> into ACL files`.

### Phase 2 — Promote `lint-acl` to enforcing

Once `reports/aclcheck.log` is empty:

1. Edit the Makefile:
   ```make
   # OLD: lint-acl writes a report and ignores exit codes
   # NEW: lint-acl runs go vet directly and fails on any violation
   lint-acl: build-aclcheck
       @bash -c 'set -e; for modfile in $$(find ./adapters -name go.mod ...); do
           dir=$$(dirname "$$modfile"); ...
           (cd "$$dir" && go vet -vettool=$(PWD)/bin/aclcheck ./...);
       done'
   ```
2. Add `lint-acl` to `lint`:
   ```make
   lint: lint-arch-check lint-gofmt lint-go-vet lint-go lint-aggregate lint-acl
   ```
3. Update `.claude/CLAUDE.md` "What each tool catches" table: mark
   `aclcheck` as **enforcing**.
4. Final `make check` must pass.

## Cost estimate

Realistic effort per adapter:

| Adapter category | Files affected | Effort |
|---|---|---|
| Native stores (sqlite outbox/dlq) | 1–3 | 30 min each |
| Credential / config adapters | 3–5 | 1 hour each |
| Transport adapters (small: amqp, sqs, mqtt) | 5–10 | 2–4 hours each |
| OTel metrics/tracing | 3–5 | 1 hour each |

Total: **~2–3 dedicated days** for the full sweep. Each adapter is
independent; you can start small to build confidence, then tackle
the larger transport adapters with the pattern firm in mind.

## Risks and how to mitigate

- **SDK type leaks into domain.** Easy to spot during the refactor:
  if a domain type has an SDK type as a field or method parameter,
  you have a deeper problem than naming. Stop and redesign.
- **Tests construct SDK types for mocks.** Tests are exempted by
  the analyzer; you don't need to refactor them. But check that any
  test helper that exposes SDK types to other test files is itself
  in an `acl_*.go` (or test) file.
- **The `acl_session.go` wrapper pattern.** Keep these wrappers
  package-private (lowercase). Exposing them re-creates the leak.
- **Analyzer false positives.** If a file legitimately imports a
  vendor that is *not* an SDK boundary (e.g., a shared utility),
  add it to a per-file allowlist in the analyzer or move it to the
  generic vendor list — but document the decision.

## Acceptance

- `reports/aclcheck.log` is empty after every adapter is refactored.
- `make lint-acl` is part of `make lint` and exits non-zero when an
  SDK import appears in a non-ACL file.
- A trial violation (drop a vendor SDK import into a non-ACL file)
  fails the check with a clear message.
- A new transport adapter scaffolded against this convention from
  day one (template: `adapters/native/store/sqliteoutbox` after
  refactor) builds without aclcheck issues.

## When NOT to start

- When you have unmerged feature branches that touch the targeted
  adapter. The file-rename will conflict with every one. Coordinate
  the freeze per adapter.
- When you don't have time to finish the chosen adapter. Half-done
  ACL refactors leave dead `acl_*.go` stubs that confuse reviewers.

## Related

- Original plan: `FINAL_DDD_HEX_CLEAN_FIX_PLAN.md` § FIX-006.
- Analyzer source: `scripts/aclcheck/analyzer.go`.
- Architectural rationale: `.claude/CLAUDE.md` § Architecture Policy.
- Sibling carve-out: `FIX-004.md` (domain bounded-context split).

## Progress

- **FIX-006-A1: Refactor adapters/native/store/sqlite{outbox,dlq} into ACL files — DONE**
  Status: ACL boundary split landed in both packages. Port-side files (`outbox.go`, `dlq.go`) carry only the `Store` struct, `Option`/`SetLogger`, `NewStore`, `Close`, public methods and trace logging — no `database/sql`, no `modernc.org/sqlite`, no blank driver import. SDK use is confined to `acl_session.go` (lifecycle + tx + Exec/Query through an unexported `sqlSession` wrapper), `acl_query.go` (DDL + parameter-bound SQL strings + clause builders), `acl_row.go` (`Rows` → domain mapping), and `acl_errors.go` (driver error classification). Schema DDL/statements/clause builders are byte-identical pre/post refactor; on-disk format unchanged. `make lint-acl` reports 0 violations under both packages (108 → 102 across the workspace); `go test -race`, `go vet`, `make lint`, `make test` all green.
- **FIX-006-A2: Refactor adapters/aws/credentials/ssm into ACL files — DONE**
  Status: ACL boundary split landed under `adapters/aws/credentials/ssm`. All `aws-sdk-go-v2/*` imports are confined to three new files: `acl_session.go` (sole holder of `aws-sdk-go-v2/aws` and `config`; defines unexported `ssmAPI` interface, `*session` with `sync.Once` lazy init, and `buildAWSConfig`), `acl_params.go` (per-call request/response wrappers `getParameter`/`getParameterVersion`/`putParameter`/`deleteParameter`/`listParameterNames`; sole user of `ssmtypes.ParameterTypeSecureString` via a `parameterValue` DTO), and `acl_errors.go` (sole holder of `ssmtypes` for `errors.As` sentinel mapping in `mapAWSError`). `repository.go` rewritten to embed `*session` and delegate via the unexported wrappers; OCC path routes through `getParameterVersion` with prior semantics preserved verbatim. Deleted `client.go` and `errors.go`. `.golangci.yml` ireturn allowlist extended with `ssmAPI` (category 5 — adapter-internal mock seam). `make lint-acl` reports 0 violations under credentials/ssm (eliminating all 7 prior offenders); `make test`, `make lint`, `go vet`, `go test -short -race -count=1` all green.
- **FIX-006-A3: Refactor adapters/aws/config/dynamodb into ACL files — DONE**
  Status: ACL boundary split landed under `adapters/aws/config/dynamodb`. All `aws-sdk-go-v2/*` SDK imports moved out of `loader.go` into four new ACL-prefixed files: `acl_session.go` (sole holder of `aws-sdk-go-v2/aws`, `config`, and the unexported `ddbAPI`/`streamsAPI` mock seams; `*session` with `sync.Once` lazy init plus `buildAWSConfig`), `acl_params.go` (per-call request/response wrappers `describeTable`, `scan`, `query`, `getItem`, `describeStream`, `getShardIterator`, `getRecords` plus the `attributeValue` DTO), `acl_streams.go` (DynamoDB Streams polling loop and shard-iterator state — orchestration over plain Go types only), and `acl_errors.go` (sole holder of `ddbtypes` for `errors.As` sentinel mapping). `loader.go` rewritten SDK-free: keeps `Loader`, `NewLoader`, public `With*` options, watch-mode wiring, and orchestration; `streams.go` deleted (-167); `streams_internal_test.go` updated for the new struct literal. Public API surface unchanged (`NewLoader`, all `With*` options, `WatchMode`). `bin/aclcheck ./adapters/aws/config/dynamodb` reports 0 violations (was 7); `make test`, `make lint`, `go vet ./...`, `go test -race -timeout 60s ./...` all green. Review: APPROVED (code-reviewer + codex synthetic-APPROVED — codex unavailable this round).
- **FIX-006-A4: Refactor adapters/amqp/transport/amqp091 and amqp10 into ACL files — DONE**
  Status: ACL boundary split landed under both `adapters/amqp/transport/amqp091` and `adapters/amqp/transport/amqp10`. Vendor SDK imports (`github.com/rabbitmq/amqp091-go`, `github.com/Azure/go-amqp`) are confined to per-package `acl_client.go`, `acl_session.go`, `acl_inbound.go`, `acl_outbound.go`, `acl_delivery.go`, and `acl_errors.go`; the port-side files `headers.go`, `session.go`, `receiver.go`, and `sender.go` are SDK-free and operate only over domain/port types and the unexported ACL seams. Errors crossing the SDK seam are wrapped via `fmt.Errorf("%w", …)` so SDK-typed sentinels never leak. Test files migrated to use the ACL mock seams; `audit/test-timing-allowlist.txt` line numbers refreshed. `bin/aclcheck` reports 0 violations under both packages; `go test -short -race -timeout 60s ./...` (both packages), `make test`, and `make lint` all green. Port interface assertions retained (`var _ ports.Receiver = (*Receiver)(nil)` etc.); `.golangci.yml` ireturn allowlist retains `amqp091.amqpConnection` and `amqp10.amqpConn`. Review: APPROVED on first pass (code-reviewer; codex: unavailable).
- **FIX-006-A5: Refactor adapters/azure/transport/servicebus into ACL files — DONE**
  Status: ACL boundary split landed under `adapters/azure/transport/servicebus`. All `github.com/Azure/azure-sdk-for-go/sdk/messaging/azservicebus` (and related `azidentity`/`azcore`) SDK imports are confined to four new ACL files: `acl_client.go` owns the SDK mock-seam interfaces (`asbAPI`, `asbSenderAPI`, `retryScheduler`), the `sessionReceiverAdapter`, `ConnectionConfig`, and the SDK constructors (`buildClient`, `rawNewAzClient`, `buildClientOptions`, `buildTLSConfig`) plus a new unexported `*asbClientHandle` wrapper exposing domain-shaped methods (`NewSender`, `NewReceiverForQueue/Subscription`, `AcceptSessionForQueue/Subscription`, `Close`); `acl_inbound.go` owns `asbDelivery` (struct + `Ack`/`Retry`/`Extend`/`autoExtendLoop`), `messageToHeaders`, `receivedToEnvelope`, and the `Receiver.receiveAndConvert`/`toDelivery` helpers; `acl_outbound.go` owns `headersToMessage`, `envelopeToMessage`, `buildRetryMessage`, plus the `sendOne`/`sendChunk` helpers (encapsulating `MessageBatch` overflow and `azservicebus.ErrMessageTooLarge` handling); `acl_errors.go` owns `MapError`/`mapServiceBusError`/`containsAny`. Port-side files `receiver.go`, `sender.go`, `factory.go`, `headers.go`, `config.go`, `credentials_refresh.go`, and `doc.go` are now SDK-import-free and operate over the unexported ACL seams plus domain/port types. Deleted `client.go`, `delivery.go`, and `errors.go` (split into the ACL files). `.golangci.yml` ireturn allowlist extended with `adapters/azure/transport/servicebus.asbAPI` (category 5: adapter-internal mock seam, matching the `amqpConnection`/`ssmAPI`/`sqsAPI` precedent). `bin/aclcheck ./adapters/azure/transport/servicebus` reports 0 violations (was 6); `go vet ./...`, `go test -short -race -count=1 -timeout 60s ./...`, `make lint`, and `make test` all green. Review: APPROVED on first pass (code-reviewer; codex: unavailable).
- **FIX-006-A6: Refactor adapters/aws/metrics/cloudwatch into ACL files — DONE**
  Status: ACL boundary split landed under `adapters/aws/metrics/cloudwatch`. All `github.com/aws/aws-sdk-go-v2/*` SDK imports are confined to three ACL files: `acl_client.go` owns the unexported `cloudWatchAPI` mock seam, the new `newCloudWatchClient` constructor, and `loadAWSConfig` (moved from `exporter.go`); `acl_alarms.go` (renamed from `alarms.go`) owns `AlarmDefinition`, `DefaultAlarms`, and `EnsureAlarms` (the SDK `PutMetricAlarmInput` construction); `acl_batcher.go` (renamed from `batcher.go`) retains the `batcher` struct, `metricData`/`aggregate` types, the histogram-aware `add` and `drain` methods, and gains four convenience entry points (`addCounter`/`addGauge`/`addHistogram`/`addTimer`) plus a `flush(ctx, client, namespace, maxBatchSize)` method that owns the `cwtypes.MetricDatum` translation, the `aws.String` namespace wrapping, and the `PutMetricData` chunking loop. Port-side `exporter.go` is now SDK-import-free: `New` delegates SDK construction to `newCloudWatchClient`; `Counter`/`Gauge`/`Histogram`/`Timer` route through the new batcher methods; `Flush` calls `e.batcher.flush(...)` instead of the deleted `sendBatched` helper. `config.go` and `doc.go` are unchanged (already SDK-free). `.golangci.yml` ireturn allowlist extended with `adapters/aws/metrics/cloudwatch.cloudWatchAPI` (category 5: adapter-internal mock seam, matching the `ssmAPI`/`sqsAPI`/`asbAPI`/`amqpConnection` precedent). `bin/aclcheck ./adapters/aws/metrics/cloudwatch` reports 0 violations (was 9); `go build ./...`, `go vet ./...`, `go test -short -race -count=1 ./...`, `make test`, and `make lint` all green. Review: APPROVED on first pass (code-reviewer; codex: unavailable).
- **FIX-006-A7: Refactor adapters/mqtt/transport/paho into ACL files — DONE**
  Status: ACL boundary split landed under `adapters/mqtt/transport/paho`. All `github.com/eclipse/paho.golang/{paho,packets,paho/log,autopaho}` imports are confined to four `acl_*.go` files: `acl_client.go` (new) defines the unexported `pahoConnection` mock-seam interface plus the production `*pahoConn` wrapper around `*autopaho.ConnectionManager`, exposing domain-shaped `AwaitConnection`, `Disconnect`, `Subscribe([]subscribeSpec)`, `Unsubscribe([]string)`, and `PublishEnvelope(*domain.Envelope, …)` methods (errors crossing the boundary are wrapped with `fmt.Errorf("paho: …: %w", err)`); `acl_session.go` (new) owns `Session.Start` (the `autopaho.ClientConfig` builder + `OnConnectionUp`/`OnConnectError`/`OnConnectionDown` callbacks + `ConnectPacketBuilder`), the `ConnectionManager()` legacy escape hatch (unwraps `cm.Underlying()`), and `classifySubackReasons` (now over `[]subscribeSpec`); `acl_router.go` (renamed from `router.go`) gains a `RegisterEnvelope(id, clk, func(*domain.Envelope))` adapter that performs `*paho.Publish → domain.Envelope` translation inside the ACL; `acl_headers.go` (renamed from `headers.go`) keeps `EnvelopeFromPublish` / `PublishFromEnvelope`. Port-side `session.go` retypes the `cm` field as `pahoConnection`; `session_lifecycle.go` keeps `Reload`/`Close` driving the seam through the interface; `session_reconcile.go` calls `cm.Subscribe`/`cm.Unsubscribe` with `subscribeSpec` literals (Topic/QoS preserved verbatim); `receiver.go` registers via `Router().RegisterEnvelope` and logs from `env.Subject` / `env.Payload` instead of the SDK `*paho.Publish`. Six tests that injected `s.cm = fakeCM` updated to `s.cm = &pahoConn{cm: fakeCM}`; `bug_reconcile_partial_success_test.go` rewritten to construct `[]subscribeSpec` literals and dropped its now-unused `pahov5` import. No new ireturn allowlist entries were needed (the unexported `pahoConnection` is consumed only through `Session.cm`; `newPahoConn` returns the concrete `*pahoConn`). `bin/aclcheck ./adapters/mqtt/transport/paho` reports 0 violations (was 11); `go build ./...`, `go vet ./...`, `go test -short -race -timeout 120s -count=1 ./...` (343 tests, 2.77s), `make test`, and `make lint` all green. Review: APPROVED on first pass (code-reviewer; codex: unavailable).
- **FIX-006-A8: Refactor adapters/aws/transport/sqs into ACL files — DONE**
  Status: ACL boundary split landed under `adapters/aws/transport/sqs`. All `github.com/aws/aws-sdk-go-v2/{aws,config,credentials,service/sqs,service/sqs/types}` imports are now confined to six `acl_*.go` files. Renamed via `git mv`: `client.go → acl_client.go` (owns the unexported `sqsAPI` mock-seam, `newSQSClient`, and AWS config loading), `credentials_refresh.go → acl_credentials.go` (credential refresh ticker + lifecycle), `delivery.go → acl_delivery.go` (auto-extend visibility, `ChangeMessageVisibility`/`DeleteMessage` calls, per-delivery cancel path), `errors.go → acl_errors.go` (`MapError` over `*types.QueueDoesNotExist`/`SenderFault`/etc.). New ACL files: `acl_inbound.go` owns the `ReceiveMessage` poll, the `pollAndConvert` helper, `convertMessage` (`*types.Message → *domain.Envelope` including `attributesToHeaders`, Subject precedence, SNS-unwrap branch, and trace logging), and `Receiver.ensureClient`; `acl_outbound.go` owns `Sender.ensureClient`, `sendOne` (single `SendMessage`), `sendBatchChunk` (single `SendMessageBatch` with chunk-owned timeout, partial-failure per-entry `ErrInvalidPayload`/`ErrUnavailable` classification), `headersToAttributes`, `extractFIFOFields`, `applyFIFO`, dedup-id derivation, and `buildBatchEntry`. Deleted: `headers.go` (helpers migrated into the in/out ACL files; nothing was left behind). Port-side `receiver.go` and `sender.go` are now SDK-import-free shells: `receiver.go` calls `r.pollAndConvert(...)` and emits per-delivery; `sender.go` keeps the chunking loop, debug log, error joining and metric emission, delegating SDK work to `sendOne`/`sendBatchChunk`. `factory.go`, `config.go`, `doc.go` unchanged (already SDK-free). No `.golangci.yml` changes required — the existing `sqsAPI` ireturn allowlist entry still applies and `newDelivery` returns the concrete `*sqsDelivery`. `bin/aclcheck ./adapters/aws/transport/sqs` reports 0 violations (was 18); `go build ./...`, `go vet ./...`, `go test -short -race -timeout 120s -count=1 ./...` (~64 tests + benchmarks in 10.4 s), `make test`, and `make lint` all green. Behavioural diff vs HEAD: byte-equivalent (poll metrics/trace-log ordering, SNS unwrap, FIFO + dedup, `ensureClient`, error classification, batch entry IDs `0..len(batch)-1` per chunk, per-chunk timeout + cancel, credential refresh lifecycle all preserved). Review: APPROVED on first pass (code-reviewer, opus 4.7; codex: unavailable).
- **FIX-006-A9: Refactor adapters/otel/{metrics,tracing} into ACL files — DONE**
  Status: ACL boundary split landed under `adapters/otel/metrics` and `adapters/otel/tracing`. All `go.opentelemetry.io/otel/*` SDK imports are now confined to per-package `acl_*.go` files. **metrics**: new `acl_client.go` owns the unexported `meterClient` mock seam, the `*otelMeterClient` production impl (holds `*sdkmetric.MeterProvider`, `metric.Meter`, the `counters`/`gauges`/`histograms` per-name caches, `defaultAttrs`, `mu`), the lazy double-checked `getOrCreate{Counter,Gauge,Histogram}` helpers, the OTLP HTTP exporter + resource + MeterProvider construction in `newMeterClient(ctx, cfg)`, and the test-friendly `newMeterClientFromProvider(mp, cfg)`. Port-side `exporter.go` is SDK-import-free: `Exporter` retains `config Config` plus the new `client meterClient`; `Counter`/`Gauge`/`Histogram`/`Timer`/`Flush`/`Close` delegate to the seam (nil-client guards return early so the `NewForTest` no-network path stays safe). **tracing**: new `acl_client.go` owns the unexported `tracerClient` seam, the `*otelTracerClient` production impl (holds `*sdktrace.TracerProvider`, `trace.Tracer`), `newTracerClient(ctx, cfg)` (OTLP HTTP exporter + resource + batched TracerProvider with `TraceIDRatioBased` sampler), `newTracerClientFromProvider(tp)`, and the SDK `Start`-call construction inside `StartSpan(ctx, name, tags)`. New `acl_span.go` owns the `*otelSpan` `ports.Span` impl (`End`, `SetError` via `codes.Error` + `RecordError`, `AddEvent`, `SetAttributes`) and the `tagsToAttributes` helper. Port-side `tracer.go` is SDK-import-free: `Tracer` retains `config Config` plus the new `client tracerClient`; `StartSpan` and `Close` delegate (a package-private `noopSpan` covers the `NewForTest` no-client path so the public `ports.Span` contract is honoured even without a provider). Test seams `export_test.go` (in both packages) updated to construct via the new `newMeterClientFromProvider`/`newTracerClientFromProvider`; `exporter_test.go` `TestExporter_BuildAttributes`/`…Empty` migrated to `TestMeterClient_*` exercising `*otelMeterClient.buildAttributes` directly (the helper moved into the ACL). No `.golangci.yml` changes required — the new unexported `meterClient` and `tracerClient` interfaces are consumed only through struct fields (not returned from constructors), so the existing `ireturn` allowlist still suffices. `bin/aclcheck ./adapters/otel/metrics ./adapters/otel/tracing` reports 0 violations (was 6 + 7 = 13); `go build ./...`, `go vet ./...`, `go test -short -race -count=1 -timeout 60s ./...` for both packages, `make test`, and `make lint` all green. Behavioural parity vs HEAD: public API (`New`, `Counter`, `Gauge`, `Histogram`, `Timer`, `Flush`, `Close`, `StartSpan`, all `With*` options) preserved verbatim, RWMutex double-checked locking on the instrument caches preserved, semconv `ServiceName`/`ServiceVersion`/`DeploymentEnvironment` resource attributes constructed identically (metrics unconditional, tracing conditional on non-empty strings as before), error-message prefixes (`otel-metrics: …`, `otel-tracing: …`) preserved by routing all wrapping through the ACL constructors and returning their errors un-rewrapped from the port-side `New`. Review: APPROVED after 1 fix-round by code-reviewer (opus 4.7) — round 0 flagged a double `otel-metrics: otel-metrics: …` / `otel-tracing: otel-tracing: …` prefix on init errors; round 1 dropped the redundant outer `fmt.Errorf` wrappers in `exporter.go` and `tracer.go` so the original prefix is preserved. Codex: unavailable.
- **FIX-006-P2: Promote lint-acl to enforcing in Makefile and update docs — DONE**
  Status: `lint-acl` is now wired into the main `lint` gate and runs `aclcheck` per adapter module under `set -eo pipefail`, so any vendor SDK import outside an `acl_*.go` file (or `acl/` sub-directory) fails the build instead of being logged. Three tail adapters not covered by A1–A9 were brought into compliance via `git mv` (whole-file ACL renames where the file is end-to-end SDK boundary, no port-side logic to peel off): `adapters/aws/store/factory.go → acl_factory.go` (DynamoDB store factory aggregator, holds `*dynamodb.Client`), `adapters/aws/store/dynamodbdlq/{errors.go,helpers.go,store.go} → acl_*.go`, `adapters/aws/store/dynamodblease/{errors.go,store.go} → acl_*.go`, `adapters/aws/store/dynamodboutbox/{errors.go,store.go} → acl_*.go`, and `adapters/native/config/file/watcher.go → acl_watcher.go` (fsnotify-based file watcher). Test files (`*_test.go`) are exempted by the analyzer (`scripts/aclcheck/analyzer.go:83`) so no test moves were needed; no `.golangci.yml` changes required. `Makefile`: rewrote the `lint-acl` recipe — old advisory body (write-and-ignore `|| true; true`) replaced with `set -eo pipefail` + `tee -a` so per-package `go vet -vettool=$(PWD)/bin/aclcheck` failures propagate; added `lint-acl` to the `lint` target's prerequisite list (`lint: lint-arch-check lint-gofmt lint-go-vet lint-go lint-aggregate lint-acl`); removed `lint-acl` from `arch-quality`'s advisory aggregator (it is no longer advisory). `.claude/CLAUDE.md`: flipped the `aclcheck` row in the "What each tool catches" table from `advisory` to `enforcing` and dropped the "(existing adapters not refactored)" qualifier; removed `aclcheck.log` from the `make arch-quality` produces-list; dropped `aclcheck` from the advisory-tools paragraph in "Why each tool exists"; updated the "Outstanding refactors" entry for FIX-006 to mark it DONE and note the analyzer is enforcing. Acceptance verified: `bin/aclcheck` reports 0 violations across all 22 adapter packages; trial violation (drop `import _ "github.com/aws/aws-sdk-go-v2/service/dynamodb/types"` into a non-ACL file) fails `make lint-acl` with `make: *** [lint-acl] Error 1` and the expected aclcheck message; `make lint` and `make test` both green end-to-end. Review: self-verified per `aclcheck` + `make lint` + `make test`; reviewer round skipped per work-tasklist user-message override (`Reviewer = code-reviewer`, "You MAY do work directly").
